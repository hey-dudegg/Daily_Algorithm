/* 
나오는 조각-> 가로+1 * 세로+1
각각의 조각들 크기 비교(max)

숫자들을 리스트에 순서에 맞게 담는다.
숫자들을 꺼내 역참조한 수에서 뺀다. 
*/

#include <stdio.h>

int main() {
	int wid, hei, cut, max;
	int* wid, * hei;
	
	scanf_s("%d %d", &wid, &hei);
	scanf_s("%d", cut);




	return 0;
}